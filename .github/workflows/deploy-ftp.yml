name: Deploy Next.js to FTP Hosting

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Create production environment file
        run: |
          if [ -n "${{ secrets.NEXT_PUBLIC_API_BASE_URL }}" ]; then
            echo "NEXT_PUBLIC_API_BASE_URL=${{ secrets.NEXT_PUBLIC_API_BASE_URL }}" > .env.production
          fi

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Prepare deploy package
        run: |
          rm -rf deploy-package
          mkdir -p deploy-package/.next
          mkdir -p deploy-package/tmp
          cp server.js deploy-package/server.js
          cp .htaccess deploy-package/.htaccess
          cp -R .next/standalone deploy-package/.next/standalone
          cp -R .next/static deploy-package/.next/static
          cp -R public deploy-package/public
          date -u +"%Y-%m-%dT%H:%M:%SZ" > deploy-package/tmp/restart.txt

      - name: Validate deploy configuration
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_SERVER_DIR: ${{ vars.FTP_SERVER_DIR }}
        run: |
          missing=0
          for key in FTP_SERVER FTP_USERNAME FTP_PASSWORD FTP_SERVER_DIR; do
            if [ -z "${!key}" ]; then
              echo "::error::$key no esta configurado en GitHub Actions"
              missing=1
            fi
          done
          exit $missing

      - name: Check FTP host DNS
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
        run: |
          node -e "const dns=require('node:dns').promises; dns.lookup(process.env.FTP_SERVER).then(()=>console.log('FTP host resuelve correctamente')).catch((err)=>{console.error('::error::No se pudo resolver el host FTP. Revisa el secret FTP_SERVER.'); console.error(err.code || err.message); process.exit(1);})"

      - name: Deploy to FTP server
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./deploy-package/
          server-dir: ${{ vars.FTP_SERVER_DIR }}
          dangerous-clean-slate: false
